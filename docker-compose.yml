services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - FAIL_HEALTH=0

  prometheus:
    image: prom/prometheus:v2.49.1
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    depends_on:
      - app
